const ContentDesc=({icon,name,title,desc,date_time})=>{
    return(
        <div>
            <h1>hi</h1>
        {/* <span><img src={icon}></img></span>
        <span>{name}</span>
        <span>{title}</span>
        <span>{desc}</span>
        <span>{date_time}</span> */}
        </div>
    )
}
export default ContentDesc;